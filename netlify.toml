# Netlify configuration for Next.js (App Router) deployment
# Uses @netlify/plugin-nextjs for full SSR, middleware, and API routes support.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
